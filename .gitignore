node_modules/
.DS_Store
tsconfig.tsbuildinfo
dist
*.d.ts
*.d.ts.map
storybook-static/
packages/cli-web/
packages/cli-sec/
packages/ngraph/
.cache/
.turbo
.env.local
sec-data/
sec-db/

# Local worktrees for subagent-driven development
.worktrees/

# Downloaded S-1 fixture cache (use `sec fetch s1-fixtures`); only the curated set under mock_data/s1/*.htm is committed
src/sec/html/mock_data/s1/.cache/
# Fixture output written by `sec fetch fixtures` / `sec fetch s1-fixtures` (relative to $SEC_FIXTURES_DIR ?? cwd)
.sec-fixtures/

# Local one-off probes / scratch files (never commit)
/*.tmp.ts
/*_probe_tmp.ts
/scratch/
